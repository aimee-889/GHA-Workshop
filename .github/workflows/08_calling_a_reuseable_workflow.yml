name: Calling a reuseable workflow
on: workflow_dispatch
jobs: 
  greet-alice: 
    uses: ./.github/workflows/greeting.yml
    with:
      username: Alice
    secrets:
      token: ${{ secrets.token }}
  greet-john: 
    uses: ./.github/workflows/greeting.yml
    with:
      username: John
    secrets:
      token: ${{ secrets.token }}
