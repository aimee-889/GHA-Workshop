  
name: Calling a reuseable workflow
on: push 
jobs: 
  greet-alice: 
    uses: ./.github/reuseable-workflows/reuseable_workflow.yml@master
    with:
      username: Alice
    secrets:
      token: ${{ secrets.token }}
  greet-john: 
    uses: ./.github/reuseable-workflows/reuseable_workflow.yml@master
    with:
      username: John
    secrets:
      token: ${{ secrets.token }}