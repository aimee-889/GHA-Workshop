name: Reusable workflow to greet someone
on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v1
      - name: Greeting Actiont 
        uses: actions/hello-world-javascript-action@v1
        with: 
          who-to-greet: ${{ inputs.username }}
